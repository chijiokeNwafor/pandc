{
  "version": "2.0.0",
  "options": {
    "cwd": "${workspaceFolder}",
    "shell": {
      "executable": "/bin/zsh",
      "args": [
        "-l",
        "-c",
        "export NVM_DIR=\"$HOME/.nvm\"; source \"$NVM_DIR/nvm.sh\" --no-use && nvm use --silent && eval \"$1\"",
        "--"
      ]
    }
  },
  "tasks": [
    {
      "type": "npm",
      "script": "dev",
      "problemMatcher": [],
      "label": "npm: dev",
      "detail": "Start the dev server using the Node version in .nvmrc"
    }
  ]
}
